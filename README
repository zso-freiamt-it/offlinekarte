# Initialize Database
Before executing the docker containers, the tiles for the map server and the addresses for searchserver must be feched. For that the following command can be executed. It is recommended to rerun this command periodically e.g. once or twice a year.

```
zskarte init
```


# Run

```
zskarte start
```
