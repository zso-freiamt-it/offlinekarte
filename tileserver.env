# Set the allowed hostnames (comma-separated)
# The tile server handles requests that come directly from the client
# browser, not other containers, so this has to be the public hostname
TILESERVER_GL_ALLOWED_HOSTS=localhost
